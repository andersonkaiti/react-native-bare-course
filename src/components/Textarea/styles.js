import { StyleSheet } from 'react-native'

export const styles = StyleSheet.create({
  textarea: {
    borderWidth: 1,
    borderColor: '#888',
    borderRadius: 8,
    height: 72,
    color: '#333',
    paddingHorizontal: 16,
    paddingTop: 16,
    paddingBottom: 16,
  },
  textareaFocused: {
    borderColor: '#222',
  },
})
