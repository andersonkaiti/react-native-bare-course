import { StyleSheet } from 'react-native'

export const styles = StyleSheet.create({
  postContainer: {
    backgroundColor: '#333',
    borderRadius: 8,
    height: 64,
    justifyContent: 'center',
    alignItems: 'center',
    flex: 1,
  },
  postTitle: {
    color: '#fff',
    fontSize: 18,
    fontWeight: 'bold',
  },
})
